<section class="hero" id="inicio">

  
  <div class="hero__bg" aria-hidden="true">
    <div class="orb orb--1"></div>
    <div class="orb orb--2"></div>
    <div class="scanline"></div>
  </div>

  <div class="container hero__inner">

   
    <div class="hero__copy">
      <p class="section-label animate-fade-up" style="animation-delay:.1s">
        Servicio 24/7 disponible
      </p>

      <h1 class="hero__title animate-fade-up" style="animation-delay:.2s">
        Electricidad que <br/>
        <span class="hero__title-highlight">conecta tu vida</span>
      </h1>

      <p class="hero__desc animate-fade-up" style="animation-delay:.35s">
        Instalaciones, reparaciones y mantenimiento eléctrico con técnicos
        certificados. Rápidos, seguros y con garantía en cada trabajo.
      </p>

      <!-- Stats strip -->
      <div class="hero__stats animate-fade-up" style="animation-delay:.45s">
        <div class="hero__stat">
          <span class="hero__stat-num">+100</span>
          <span class="hero__stat-label">Proyectos</span>
        </div>
        <div class="hero__stat-divider"></div>
        <div class="hero__stat">
          <span class="hero__stat-num">5 años</span>
          <span class="hero__stat-label">Experiencia</span>
        </div>
        <div class="hero__stat-divider"></div>
        <div class="hero__stat">
          <span class="hero__stat-num">100%</span>
          <span class="hero__stat-label">Garantizado</span>
        </div>
      </div>

      <div class="hero__actions animate-fade-up" style="animation-delay:.55s">
        <a href="#servicios" class="btn btn--primary btn--lg">
          Ver servicios
          <svg width="18" height="18" viewBox="0 0 24 24" fill="currentColor">
            <path d="M13.22 19.03a.75.75 0 0 1 0-1.06L18.19 13H3.75a.75.75 0 0 1 0-1.5h14.44l-4.97-4.97a.75.75 0 1 1 1.06-1.06l6.25 6.25a.75.75 0 0 1 0 1.06l-6.25 6.25a.75.75 0 0 1-1.06 0Z"/>
          </svg>
        </a>
        <a href="https://wa.me/593000000000" target="_blank" rel="noopener" class="btn btn--outline btn--lg">
          <svg width="18" height="18" viewBox="0 0 24 24" fill="currentColor">
            <path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347Z"/>
            <path d="M12 0C5.373 0 0 5.373 0 12c0 2.625.846 5.059 2.284 7.05L.785 23.493a.5.5 0 0 0 .622.622l4.443-1.499A11.95 11.95 0 0 0 12 24c6.627 0 12-5.373 12-12S18.627 0 12 0Zm0 21.818a9.827 9.827 0 0 1-5.01-1.37l-.36-.213-3.717 1.253 1.253-3.717-.213-.36A9.827 9.827 0 0 1 2.182 12C2.182 6.57 6.57 2.182 12 2.182S21.818 6.57 21.818 12 17.43 21.818 12 21.818Z"/>
          </svg>
          WhatsApp
        </a>
      </div>
    </div>

    
    <div class="hero__visual">
      <div class="hero__card">
        
        <img
          src="/images/electricista.png"
          alt="Técnico electricista Electro Life"
          class="hero__card-img"
          width="520" height="600"
          loading="eager"
          onerror="this.parentElement.classList.add('hero__card--no-img')"
        />
        
        <div class="hero__badge hero__badge--tl">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="var(--clr-primary)">
            <path d="M12 1L3 5v6c0 5.55 3.84 10.74 9 12 5.16-1.26 9-6.45 9-12V5l-9-4Z"/>
          </svg>
          <span>Certificado</span>
        </div>
        <div class="hero__badge hero__badge--br">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="var(--clr-accent)">
            <path d="M13 2L4.09 12.11h7.91L10 22l9.9-10.24H12.5L13 2Z"/>
          </svg>
          <span>24/7 disponible</span>
        </div>
      </div>
    </div>

  </div>

  
  <div class="hero__scroll" aria-hidden="true">
    <div class="hero__scroll-line"></div>
    <span>Scroll</span>
  </div>

</section>

<style>
  .hero {
    position: relative;
    min-height: 100dvh;
    display: flex;
    align-items: center;
    padding-top: 5rem;
    overflow: hidden;
  }

  /* Animated background */
  .hero__bg { position: absolute; inset: 0; pointer-events: none; }

  .orb {
    position: absolute;
    border-radius: 50%;
    filter: blur(80px);
  }

  .orb--1 {
    width: 600px; height: 600px;
    top: -200px; right: -150px;
    background: radial-gradient(circle, rgba(0,200,255,0.12) 0%, transparent 70%);
    animation: float 8s ease-in-out infinite;
  }

  .orb--2 {
    width: 400px; height: 400px;
    bottom: -100px; left: -100px;
    background: radial-gradient(circle, rgba(0,255,204,0.06) 0%, transparent 70%);
    animation: float 11s ease-in-out infinite reverse;
  }

  .scanline {
    position: absolute;
    left: 0; right: 0;
    height: 2px;
    background: linear-gradient(90deg, transparent, rgba(0,200,255,0.08), transparent);
    animation: scanline 5s linear infinite;
    pointer-events: none;
  }

  /* Layout */
  .hero__inner {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
    padding-block: clamp(3rem, 6vw, 5rem);
  }

  /* Copy */
  .hero__title {
    font-family: var(--font-heading);
    font-size: clamp(2.4rem, 5.5vw, 4.2rem);
    font-weight: 700;
    line-height: 1.08;
    margin-bottom: 1.5rem;
    letter-spacing: -0.01em;
  }

  .hero__title-highlight {
    font-family: var(--font-display);
    color: var(--clr-primary);
    text-shadow: var(--glow-lg);
    font-size: 0.88em;
  }

  .hero__desc {
    font-size: 1.05rem;
    color: var(--clr-text-muted);
    max-width: 480px;
    margin-bottom: 2.5rem;
    line-height: 1.7;
  }

  /* Stats */
  .hero__stats {
    display: flex;
    align-items: center;
    gap: 1.5rem;
    margin-bottom: 2.5rem;
    padding: 1.25rem 1.5rem;
    background: var(--clr-surface);
    border: 1px solid var(--clr-border);
    border-radius: 8px;
    backdrop-filter: blur(8px);
  }

  .hero__stat { text-align: center; }

  .hero__stat-num {
    display: block;
    font-family: var(--font-display);
    font-size: 1.4rem;
    font-weight: 700;
    color: var(--clr-primary);
    text-shadow: var(--glow-sm);
    line-height: 1;
    margin-bottom: 0.25rem;
  }

  .hero__stat-label {
    font-family: var(--font-heading);
    font-size: 0.75rem;
    font-weight: 600;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--clr-text-muted);
  }

  .hero__stat-divider {
    width: 1px;
    height: 2.5rem;
    background: var(--clr-border);
  }

  /* Actions */
  .hero__actions {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
  }

  /* Visual */
  .hero__visual {
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .hero__card {
    position: relative;
    border-radius: 16px;
    overflow: visible;
    max-width: 420px;
    width: 100%;
    animation: float 6s ease-in-out infinite;
  }

  .hero__card-img {
    width: 100%;
    height: 500px;
    object-fit: cover;
    border-radius: 16px;
    border: 1px solid var(--clr-border);
    box-shadow: var(--glow-lg), 0 40px 80px rgba(0,0,0,0.5);
    display: block;
  }

  /* Fallback when no image */
  .hero__card--no-img::after {
    content: '';
    display: block;
    height: 500px;
    border-radius: 16px;
    border: 1px solid var(--clr-border);
    background:
      linear-gradient(135deg, var(--clr-surface) 0%, var(--clr-surface-2) 100%);
    box-shadow: var(--glow-lg);
  }

  /* Floating badges */
  .hero__badge {
    position: absolute;
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.6rem 1rem;
    background: rgba(4, 15, 31, 0.92);
    border: 1px solid var(--clr-border);
    border-radius: 50px;
    backdrop-filter: blur(12px);
    box-shadow: var(--glow-sm);
    font-family: var(--font-heading);
    font-size: 0.8rem;
    font-weight: 700;
    letter-spacing: 0.05em;
    white-space: nowrap;
  }

  .hero__badge--tl { top: 1.5rem; left: -1.5rem; }
  .hero__badge--br { bottom: 2rem;  right: -1.5rem; }

  /* Scroll hint */
  .hero__scroll {
    position: absolute;
    bottom: 2rem;
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
    color: var(--clr-text-muted);
    font-family: var(--font-display);
    font-size: 0.55rem;
    letter-spacing: 0.2em;
    text-transform: uppercase;
  }

  .hero__scroll-line {
    width: 1px;
    height: 3rem;
    background: linear-gradient(to bottom, var(--clr-primary), transparent);
    animation: pulse-glow 2s ease-in-out infinite;
  }

  /* Responsive */
  @media (max-width: 900px) {
    .hero__inner {
      grid-template-columns: 1fr;
      text-align: center;
    }

    .section-label { justify-content: center; }
    .hero__desc    { margin-inline: auto; }
    .hero__actions { justify-content: center; }
    .hero__stats   { justify-content: center; }

    .hero__visual { order: -1; }

    .hero__card { max-width: 340px; }
    .hero__card-img { height: 380px; }
    .hero__badge--tl { left: 0; }
    .hero__badge--br { right: 0; }
  }
</style>
